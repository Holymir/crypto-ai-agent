require('./scheduler/cronJob');


// require('dotenv').config();
// const fetchNews = require('./services/newsFetcher');
// const analyzeSentiment = require('./services/sentimentAnalyzer');
// const logger = require('./services/logger');

// (async () => {
//   const articles = await fetchNews();

//   for (const article of articles) {
//     const sentiment = await analyzeSentiment(article.title + "\n\n" + article.content);
//     logger.log(article.title, sentiment);
//   }
// })();
